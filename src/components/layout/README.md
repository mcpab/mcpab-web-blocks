# Layout

Layout-oriented building blocks used to assemble page sections.

## Exports

- `BackgroundBox`: background media + overlay wrapper.
- `PageLayout`: page-level structural wrapper.
- `sections`: composed section blocks (`HeroBlock`, `MediaText`).
