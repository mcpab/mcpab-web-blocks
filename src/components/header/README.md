# Header

Composable site header with responsive navigation support.

## Exports

- `Header`: primary header component.
- Header prop types for grouped and legacy configuration shapes.

Depends on hierarchy/menu types from `src/components/menus`.
