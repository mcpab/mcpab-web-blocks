# Core

Framework-agnostic domain and adapter layer.

## Modules

- `hierarchy`: typed hierarchy model + validation + d3 transform pipeline.
- `image`: universal image component interfaces and helpers.
- `link`: universal link component interface.
- `script`: universal script component interface.

These modules are intended to be reused by components and by external adapters.
