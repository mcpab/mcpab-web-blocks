# Lib

Cross-cutting utilities used by components and policies.

## Modules

- `text`: inline markdown parsing and string transforms.
- `icon`: icon lookup/picker helpers for UI labels.
- `utils`: shared string helpers (`camelCase`, `safeTitleCase`).
