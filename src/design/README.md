# Design

Design tokens and token-to-style helpers.

## Exports

- `SECTION_MIN_H`: section size token map.
- `SectionSize`: section size token type.
- `sectionMinHeightSx`: helper returning MUI `sx` min-height object.
